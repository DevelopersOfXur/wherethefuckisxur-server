{{!< data}}
<div class="vendor-info">
    <h1 class="page-title">
        {{display.name}}
    </h1>
    <h3 class="vendor-desc">
        {{desc}}
    </h3>
    <div class="vendor-dropdown">
        {{#each vendors}}
        <a class="vendor-option" href="/data/vendor/{{hash}}">
            {{name}}
        </a>
        {{/each}}
    </div>
</div>
<div class="vendor-sales">
    {{#each categories}}
    {{> vendorcat}}
    {{/each}}
</div>
